(() => {
  "use strict";
  const page = document.body.dataset.page;
  const gp = new Intl.NumberFormat("en-GB", { maximumFractionDigits: 0 });
  const pct = new Intl.NumberFormat("en-GB", { maximumFractionDigits: 1 });
  const esc = (v) => String(v ?? "").replace(/[&<>'\"]/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[c]);
  const plainGp = (v) => v == null ? "-" : gp.format(v);
  const gpText = (v) => v == null ? "Unavailable" : `${gp.format(v)} gp`;
  const membership = (m) => m ? "P2P" : "F2P";
  const badge = (text, klass="") => `<span class="badge ${esc(klass)}">${esc(text)}</span>`;
  const riskBadge = (risk) => badge(risk?.label || risk?.level || "Unknown", risk?.level || "");
  async function loadJson(path){const r=await fetch(path,{cache:"no-store"});if(!r.ok)throw new Error(`${path}: ${r.status}`);return r.json();}
  function relativeAge(s){s=Math.max(0,Number(s||0));if(s<60)return"Updated less than a minute ago";if(s<3600)return`Updated ${Math.floor(s/60)} min ago`;if(s<86400)return`Updated ${Math.floor(s/3600)} hr ago`;return`Updated ${Math.floor(s/86400)} day${s>=172800?"s":""} ago`;}
  async function initStatus(){try{const s=await loadJson("data/status.json"),now=Math.floor(Date.now()/1000),age=Math.max(Number(s.ageSeconds||0),now-Number(s.generatedAt||now)),node=document.querySelector("#health-state");node.textContent=({current:"Current",delayed:"Delayed",data_issue:"Data issue"})[s.state]||"Data issue";node.className=`status-plaque ${s.state||"data_issue"}`;document.querySelector("#update-age").textContent=s.state==="data_issue"?"Showing last good public dataset":relativeAge(age);}catch(_){document.querySelector("#health-state").textContent="Data issue";document.querySelector("#health-state").className="status-plaque data_issue";document.querySelector("#update-age").textContent="Status unavailable";}}
  function featuredAfkHtml(i){if(!i)return`<p class="empty-state">No reliable current AFK calculation is available.</p>`;return`<div class="panel-heading"><span class="kicker">Featured AFK · ${esc(i.category)}</span><h2>${esc(i.name)}</h2></div><p class="metric-primary">${plainGp(i.currentGpPerHour)}</p><p class="metric-caption">GP per hour at sustainable throughput</p><div class="stat-grid"><div class="stat"><span>24H reference</span><strong>${gpText(i.reference24hGpPerHour)}</strong></div><div class="stat"><span>AFK interval</span><strong>${i.afkIntervalSeconds==null?"-":`${plainGp(i.afkIntervalSeconds)} sec`}</strong></div><div class="stat"><span>GP / interaction</span><strong>${gpText(i.gpPerInteraction)}</strong></div></div><div class="entry-meta">${badge(membership(i.members))}${badge(i.risk,i.risk)}<span>1h capital: <strong>${gpText(i.capitalOneHour)}</strong></span></div><p class="reason">${esc(i.reason)}</p><a class="action-link" href="afk.html?method=${encodeURIComponent(i.methodId)}">Browse AFK Methods</a>`;}
  function featuredAlchHtml(i){if(!i)return`<p class="empty-state">No reliable current High Alch candidate is available.</p>`;return`<div class="ticket-heading"><span class="kicker">High Alch trade ticket</span><h2>${esc(i.name)}</h2></div><p class="metric-primary">${plainGp(i.profit4h)}</p><p class="metric-caption">Practical four-hour profit</p><div class="stat-grid"><div class="stat"><span>Profit / cast</span><strong>${gpText(i.profitPerCast)}</strong></div><div class="stat"><span>4H quantity</span><strong>${plainGp(i.quantity4h)}</strong></div><div class="stat"><span>Capital</span><strong>${gpText(i.capitalRequired)}</strong></div></div><div class="entry-meta">${badge(membership(i.members))}${badge(i.freshness)}${badge(i.risk,i.risk)}</div><a class="action-link" href="alchemy.html">Open High Alch</a>`;}
  async function initDashboard(){try{const d=await loadJson("data/dashboard.json"),a=document.querySelector("#featured-afk"),h=document.querySelector("#featured-alch");a.classList.remove("loading-panel");h.classList.remove("loading-panel");a.innerHTML=featuredAfkHtml(d.featuredAfk);h.innerHTML=featuredAlchHtml(d.featuredAlchemy);document.querySelector("#notices").innerHTML=(d.notices||[]).length?d.notices.map(n=>`<div class="notice-entry"><span class="notice-kind">${esc(n.kind)}</span><span class="notice-title">${esc(n.title)}</span><span class="notice-text">${esc(n.text)}</span></div>`).join(""):`<p class="empty-state">No notable market notices in the current dataset.</p>`;}catch(_){document.querySelector("#featured-afk").innerHTML=`<p class="empty-state">Featured AFK data could not be loaded.</p>`;document.querySelector("#featured-alch").innerHTML=`<p class="empty-state">High Alch data could not be loaded.</p>`;}}
  const readRadio=(n)=>document.querySelector(`input[name="${n}"]:checked`)?.value||"all";
  function syncQuery(values){const p=new URLSearchParams();Object.entries(values).forEach(([k,v])=>{if(v!=null&&v!==""&&v!=="all"&&v!=="profitable"&&v!=="gp-hour"&&v!=="profit-4h"&&v!=="0")p.set(k,v)});history.replaceState(null,"",`${location.pathname}${p.toString()?`?${p}`:""}`)}
  function requirementsHtml(r){const s=Object.entries(r?.skills||{}).map(([n,l])=>`<li>${esc(n[0].toUpperCase()+n.slice(1))}: ${esc(l)}</li>`).join(""),q=(r?.quests||[]).map(x=>`<li>${esc(x)}</li>`).join(""),e=(r?.equipment||[]).map(x=>`<li>${esc(x)}</li>`).join("");return`${s?`<h3>Skills</h3><ul class="detail-list">${s}</ul>`:""}${q?`<h3>Quests</h3><ul class="detail-list">${q}</ul>`:""}${e?`<h3>Equipment</h3><ul class="detail-list">${e}</ul>`:""}`||"<p>No additional requirements listed.</p>";}
  function itemsHtml(title,items){return items?.length?`<h3>${esc(title)}</h3><ul class="detail-list">${items.map(x=>`<li>${esc(x.quantity)} × ${esc(x.name)}</li>`).join("")}</ul>`:"";}
  function afkRecord(m){const cur=m.current.gpPerHour,ref=m.history?.["24hGpPerHour"],cls=cur!=null&&cur<0?"loss":"profit";return`<details class="ledger-record" data-method-id="${esc(m.methodId)}"><summary class="ledger-summary afk-grid"><div class="method-name"><strong>${esc(m.name)}</strong><small>${esc(m.category)} · ${esc(m.afk.classification)} · ${membership(m.members)}</small></div><div class="num primary-mobile ${cls}">${plainGp(cur)}</div><div class="num">${plainGp(ref)}</div><div class="num">${m.afk.intervalSeconds==null?"-":`${plainGp(m.afk.intervalSeconds)}s`}</div><div class="num">${plainGp(m.afk.gpPerInteraction)}</div><div class="num desktop-secondary">${plainGp(m.economics.capitalOneHour)}</div><div class="desktop-secondary">${membership(m.members)}</div><div class="desktop-secondary">${riskBadge(m.risk)}</div></summary><div class="detail-panel"><div class="badge-row">${badge(m.afk.classification)}${(m.tags||[]).map(t=>badge(t)).join("")}${riskBadge(m.risk)}</div><p>${esc(m.description||m.afk.description||"")}</p><div class="detail-grid"><div>${requirementsHtml(m.requirements)}</div><div>${itemsHtml("Inputs",m.inputs)}${itemsHtml("Outputs",m.outputs)}</div><div><h3>Economics</h3><p>1h capital: <strong>${gpText(m.economics.capitalOneHour)}</strong></p><p>4h capital: <strong>${gpText(m.economics.capitalFourHours)}</strong></p><p>GE buy-limit constrained: <strong>${m.economics.buyLimitConstrained?"Yes":"No"}</strong></p><p>${esc(m.risk?.reasons?.[0]||"No material current warning.")}</p></div></div><div class="history-grid"><div class="history-cell"><span>Current</span><strong>${gpText(cur)}</strong></div><div class="history-cell"><span>6H</span><strong>${gpText(m.history?.["6hGpPerHour"])}</strong></div><div class="history-cell"><span>24H</span><strong>${gpText(ref)}</strong></div><div class="history-cell"><span>7D</span><strong>${gpText(m.history?.["7dGpPerHour"])}</strong></div><div class="history-cell"><span>30D</span><strong>${gpText(m.history?.["30dGpPerHour"])}</strong></div></div>${m.reference?`<a class="action-link" href="${esc(m.reference)}" target="_blank" rel="noopener noreferrer">Source / reference</a>`:""}</div></details>`;}
  async function initAfk(){const list=document.querySelector("#afk-list");try{const data=await loadJson("data/afk.json"),methods=data.methods||[],cat=document.querySelector("#afk-category"),params=new URLSearchParams(location.search),cats=[...new Set(methods.map(m=>m.category))].sort();cat.insertAdjacentHTML("beforeend",cats.map(c=>`<option value="${esc(c)}">${esc(c)}</option>`).join(""));document.querySelector("#afk-search").value=params.get("q")||"";if(params.get("category")&&cats.includes(params.get("category")))cat.value=params.get("category");if(["f2p","members"].includes(params.get("members")))document.querySelector(`input[name="afk-membership"][value="${params.get("members")}"]`).checked=true;if(params.get("profit")==="all")document.querySelector("#afk-profit").value="all";if(params.get("level"))document.querySelector("#afk-level").value=params.get("level");if(params.get("sort"))document.querySelector("#afk-sort").value=params.get("sort");const render=()=>{const search=document.querySelector("#afk-search").value.trim().toLowerCase(),cv=cat.value,mv=readRadio("afk-membership"),pv=document.querySelector("#afk-profit").value,lv=document.querySelector("#afk-level").value,sort=document.querySelector("#afk-sort").value;let rows=methods.filter(m=>!(search&&!`${m.name} ${m.category} ${(m.tags||[]).join(" ")}`.toLowerCase().includes(search))&&!(cv!=="all"&&m.category!==cv)&&!(mv==="f2p"&&m.members)&&!(mv==="members"&&!m.members)&&!(pv==="profitable"&&!(m.current.valid&&Number(m.current.gpPerHour)>0))&&!(lv!=="all"&&m.afk.classification!==lv));const val=(m,k)=>({"gp-hour":m.current.gpPerHour,"gp-24h":m.history?.["24hGpPerHour"],"gp-interaction":m.afk.gpPerInteraction,"afk-interval":m.afk.intervalSeconds,capital:m.economics.capitalOneHour})[k];rows.sort((a,b)=>sort==="alphabetical"?a.name.localeCompare(b.name):sort==="capital"?(val(a,sort)??Infinity)-(val(b,sort)??Infinity):(val(b,sort)??-Infinity)-(val(a,sort)??-Infinity));document.querySelector("#afk-count").textContent=`${rows.length} method${rows.length===1?"":"s"}`;list.innerHTML=`<div class="ledger-header afk-grid"><div>Method</div><div class="num">Current GP/h</div><div class="num">24H GP/h</div><div class="num">AFK interval</div><div class="num">GP / interaction</div><div class="num desktop-secondary">1h capital</div><div class="desktop-secondary">Access</div><div class="desktop-secondary">Risk</div></div>${rows.length?rows.map(afkRecord).join(""):`<p class="empty-state">No methods match these filters.</p>`}`;syncQuery({q:search,category:cv,members:mv,profit:pv,level:lv,sort});const requested=params.get("method");if(requested)list.querySelector(`[data-method-id="${CSS.escape(requested)}"]`)?.setAttribute("open","");};document.querySelectorAll("#afk-category,#afk-profit,#afk-level,#afk-sort,input[name='afk-membership']").forEach(el=>el.addEventListener("change",render));document.querySelector("#afk-search").addEventListener("input",render);render();}catch(_){list.innerHTML=`<p class="empty-state">AFK method data could not be loaded.</p>`;}}
  function alchRecord(i){const cls=i.profitPerCast!=null&&i.profitPerCast<0?"loss":"profit";return`<details class="ledger-record"><summary class="ledger-summary alch-grid"><div class="item-name"><strong>${esc(i.name)}</strong><small>${membership(i.members)} · ${esc(i.freshness)}</small></div><div class="num primary-mobile ${cls}">${plainGp(i.profit4h)}</div><div class="num">${plainGp(i.buyPrice)}</div><div class="num">${plainGp(i.highAlchValue)}</div><div class="num ${cls}">${plainGp(i.profitPerCast)}</div><div class="num desktop-secondary">${i.roi==null?"-":`${pct.format(i.roi)}%`}</div><div class="num desktop-secondary">${plainGp(i.quantity4h)}</div><div class="num desktop-secondary ${cls}">${plainGp(i.profit4h)}</div><div class="num desktop-secondary">${plainGp(i.capitalRequired)}</div><div class="desktop-secondary">${esc(i.freshness)}</div></summary><div class="detail-panel"><div class="badge-row">${badge(membership(i.members))}${badge(i.freshness)}${riskBadge(i.risk)}</div><div class="detail-grid"><div><h3>Current calculation</h3><p>Buy price: <strong>${gpText(i.buyPrice)}</strong></p><p>High Alch value: <strong>${gpText(i.highAlchValue)}</strong></p><p>Rune cost: <strong>${gpText(i.runeCost)}</strong></p><p>Profit/cast: <strong>${gpText(i.profitPerCast)}</strong></p></div><div><h3>Four-hour batch</h3><p>Available quantity: <strong>${plainGp(i.quantity4h)}</strong></p><p>GE buy limit: <strong>${plainGp(i.buyLimit)}</strong></p><p>Capital required: <strong>${gpText(i.capitalRequired)}</strong></p><p>Practical profit: <strong>${gpText(i.profit4h)}</strong></p></div><div><h3>Market context</h3><p>24H volume: <strong>${plainGp(i.volume24h)}</strong></p><p>24H reference margin: <strong>${gpText(i.history24hProfitPerCast)}</strong></p><p>Requirement: <strong>55 Magic</strong></p><p>${esc(i.risk?.reasons?.[0]||"No material current warning.")}</p></div></div></div></details>`;}
  async function initAlchemy(){const list=document.querySelector("#alch-list");try{const data=await loadJson("data/alchemy.json"),items=data.items||[];const render=()=>{const search=document.querySelector("#alch-search").value.trim().toLowerCase(),mv=readRadio("alch-membership"),pv=document.querySelector("#alch-profit").value,min=Number(document.querySelector("#alch-min-profit").value||0),sort=document.querySelector("#alch-sort").value,show=document.querySelector("#alch-unavailable").checked;let rows=items.filter(i=>!(search&&!i.name.toLowerCase().includes(search))&&!(mv==="f2p"&&i.members)&&!(mv==="members"&&!i.members)&&!(!show&&i.profitPerCast==null)&&!(pv==="profitable"&&!(Number(i.profitPerCast)>0))&&!(i.profitPerCast!=null&&Number(i.profitPerCast)<min));const val=(i,k)=>({"profit-4h":i.profit4h,"profit-cast":i.profitPerCast,roi:i.roi,capital:i.capitalRequired,volume:i.volume24h})[k];rows.sort((a,b)=>sort==="capital"?(val(a,sort)??Infinity)-(val(b,sort)??Infinity):(val(b,sort)??-Infinity)-(val(a,sort)??-Infinity));document.querySelector("#alch-count").textContent=`${rows.length} candidate${rows.length===1?"":"s"}`;list.innerHTML=`<div class="ledger-header alch-grid"><div>Item</div><div class="num">4H profit</div><div class="num">Buy</div><div class="num">Alch</div><div class="num">Profit/cast</div><div class="num desktop-secondary">ROI</div><div class="num desktop-secondary">4H qty</div><div class="num desktop-secondary">4H profit</div><div class="num desktop-secondary">Capital</div><div class="desktop-secondary">Freshness</div></div>${rows.length?rows.map(alchRecord).join(""):`<p class="empty-state">No High Alch candidates match these filters.</p>`}`;syncQuery({q:search,members:mv,profit:pv,min:String(min),sort,unavailable:show?"1":""});};document.querySelectorAll("#alch-profit,#alch-sort,#alch-unavailable,input[name='alch-membership']").forEach(el=>el.addEventListener("change",render));document.querySelector("#alch-search").addEventListener("input",render);document.querySelector("#alch-min-profit").addEventListener("input",render);render();}catch(_){list.innerHTML=`<p class="empty-state">High Alch data could not be loaded.</p>`;}}
  initStatus();if(page==="dashboard")initDashboard();if(page==="afk")initAfk();if(page==="alchemy")initAlchemy();
})();
